import { CopyToClipboard } from 'react-copy-to-clipboard';
import { FaRegFolder, FaExternalLinkAlt } from 'react-icons/fa';
interface Props {
	title: string;
	description: string;
	url: string;
	clone_url: string;
	languages: string[][];
}


const Project = ({ title, description, url, clone_url, languages }: Props) => {
	return (
		<div className="col">
			<div className="project-card card h-100">
				<div className="card-body">
					<div className="d-flex justify-content-between align-items-start mb-3">
						<div className="p-2 rounded">
							<FaRegFolder />
						</div>
						<div className="d-flex gap-2">
							<a href={url} target="_blank"><FaExternalLinkAlt /></a>
						</div>
					</div>
					<h5 className="card-title">{title}</h5>
					<p className="card-text">{description}</p>
					<a href={url} target="_blank" className="card-link">Github Repository</a>
					<CopyToClipboard text={clone_url}>
						<button className="card-link">Clone the Project</button>
					</CopyToClipboard>

					{languages.map((language, index) =>
						<label key={index}>{lanuage}</label>
					)}
				</div>
			</div>
		</div>
	);
}

export default Project;

